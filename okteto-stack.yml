services:
  filmbot:
    build: .
    environment:
      BOT_TOKEN :$ABOT_TOKEN  
      TMDB_TOKEN: $TMDB_TOKEN  
    ports:
      - 8080
    resources:
      cpu: 1000m
      memory: 3Gi
    command: node app.js
